{
  "name": "sampleman",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "build": "mkdir -p build; rsync -Rr * build/"
  },
  "dependencies": {
    "cookie-parser": ">=1.4.3",
    "express": ">=4.15.5",
    "marked": ">=0.3.6",
    "morgan": ">=1.9.0",
    "multer": ">=1.3.0",
    "pug": ">=2.0.0",
    "serve-favicon": ">=2.4.5",
    "www": ">=0.0.0"
  }
}
